//Used for toggling and animating the playlist bar
function togglePlaylist() {

	// $('#nav-playlist').animate({width: 'toggle'},"slow");
	// $('#nav-playlist').animate({opacity: 'toggle'},"slow");

	// $('#nav-playlist').css('display', "inline-block");
	// $('#nav-button').css('background-image','linear-gradient(45deg, transparent 13px, #ccc 13px, #ccc 15px, transparent 0), linear-gradient(-45deg, white 13px, #ccc 13px, #ccc 15px, white 0');
}